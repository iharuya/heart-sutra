{
	"name": "heart-sutra",
	"type": "module",
	"scripts": {
		"fix": "biome check --apply --diagnostic-level=error .",
		"fix:strict": "biome check --apply --diagnostic-level=info .",
		"fix:unsafe": "biome check --apply-unsafe --verbose ."
	},
	"devDependencies": {
		"@biomejs/biome": "^1.7.3",
		"bun-types": "latest"
	},
	"peerDependencies": {
		"typescript": "^5.0.0"
	}
}
